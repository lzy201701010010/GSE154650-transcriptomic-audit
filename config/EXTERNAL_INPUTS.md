# External input configuration

The package does not redistribute the GSE154650 raw archive, processed per-patient files, Ensembl GTF or miRBase sequence files. Obtain them from their official repositories, preserve the filenames expected by the portable manifest, and verify SHA-256 values before running T1. No automatic download is performed.
